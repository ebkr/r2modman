package globals

// SelectedProfile : Used to reference the currently selected profile
var SelectedProfile string

// ExecutableLocation / RootDirectory : Used to reference where the actual EXE is located
var ExecutableLocation string
var RootDirectory string

// ROR2MMProtocol : Set if application was launched using the ror2mm protocol
var ROR2MMProtocol string
